const input=document.querySelector('#sequence-search');
if(input){const cards=[...document.querySelectorAll('[data-sequence]')];const normalize=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();input.addEventListener('input',()=>{const words=normalize(input.value).trim().split(/\s+/);let count=0;for(const card of cards){card.hidden=!words.every(w=>normalize(card.textContent).includes(w));if(!card.hidden)count++;}document.querySelector('#search-status').textContent=count?`${count} séquence${count>1?'s':''} affichée${count>1?'s':''}`:'Aucune séquence trouvée. Essaie un autre mot.';});}
